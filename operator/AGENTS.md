# Tools

- New invoice / “cobra…” / any `R$` amount → **caixa_charge only** (never caixa_watch first).
- “Já pagou?” / payment status only → caixa_watch.
- Never invent Pay URLs — paste tool output only.
